# Game Server Configuration

# PUBLIC_GAMESERVER_URL is used by browser for client-side requests
PUBLIC_GAMESERVER_URL=http://localhost:3000

# GAMESERVER_URL is used by SvelteKit server for SSR requests
# Must be an absolute URL accessible from the server
GAMESERVER_URL=http://localhost:3000/gameserver

# For testing without gameserver, you can temporarily point to a mock server
# PUBLIC_GAMESERVER_URL=https://jsonplaceholder.typicode.com